# Permite instalar dependências com peer deps antigas (React 19 / Expo)
# sem bloquear o install — mesmo padrão do WebRestaurant. Necessário para
# o EAS Build (fase Install dependencies) e para o install local.
legacy-peer-deps=true
